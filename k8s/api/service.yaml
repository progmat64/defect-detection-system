apiVersion: v1
kind: Service
metadata:
  name: defect-detection-api
  labels:
    app: defect-detection-api
spec:
  type: NodePort
  selector:
    app: defect-detection-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000